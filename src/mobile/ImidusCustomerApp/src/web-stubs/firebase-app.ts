/**
 * Web stub for @react-native-firebase/app
 * Provides no-op implementation for web preview
 */

const firebase = {
  app: () => ({
    name: 'web-preview',
  }),
  apps: [],
  initializeApp: () => {},
  SDK_VERSION: 'web-preview',
};

export default firebase;
