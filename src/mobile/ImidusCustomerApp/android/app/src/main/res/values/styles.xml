<?xml version="1.0" encoding="utf-8"?>
<!--
  IMIDUS Technologies – Android Styles
  Path: android/app/src/main/res/values/styles.xml
-->
<resources>

    <!-- Base application theme -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:statusBarColor">@color/brandBlue</item>
        <item name="android:navigationBarColor">@color/brandBlue</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="colorPrimary">@color/brandBlue</item>
        <item name="colorPrimaryDark">@color/darkBg</item>
        <item name="colorAccent">@color/brandGold</item>
    </style>

    <!--
      Splash screen theme — shown before React Native JS bundle loads.
      Background is brand blue; the centre image is rendered by
      react-native-splash-screen or the Expo splash module.
    -->
    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_background</item>
        <item name="android:statusBarColor">@color/brandBlue</item>
        <item name="android:navigationBarColor">@color/brandBlue</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <!-- Boot / launch activity theme (no title bar) -->
    <style name="BootTheme" parent="AppTheme">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">false</item>
    </style>

</resources>
